<li class="shadowbox">
    <h2>{L_WEBSITE_TITLE}</h2>
    <div class="sbcontent">
        <form action="***form_url***" id="norepoform" method="post" enctype="multipart/form-data">
            <div>{L_WEBSITE_NOREPOS}</div>
            <dl class="input">
                <dt><label for="web-repos" class="required">{L_WEBSITE_REPOS}</label></dt>
                <dd><input class="fillwide" type="text" name="web-repos" id="web-repos" size="100" title="{L_WEBSITE_REPOSEG}" value="***web-repos***" /></dd>
            </dl>
            <div class="submitbox">
                <input type="submit" class="button blue" value="{L_WEBSITE_SETREPOS}" name="setrepos" />
            </div>
        </form>
    </div>
</li>
